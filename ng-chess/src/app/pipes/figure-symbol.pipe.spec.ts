import { FigureSymbolPipe } from './figure-symbol.pipe';

describe('FigureSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new FigureSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
